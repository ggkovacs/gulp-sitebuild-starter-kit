{
  "private": true,
  "scripts": {
    "start": "gulp watch",
    "compile": "gulp compile --production",
    "test": "eslint gulpfile.js && gulp && git status | grep 'working directory clean' >/dev/null || (echo 'Please commit all changes generated by building'; exit 1)"
  },
  "devDependencies": {
    "apache-server-configs": "^2.14.0",
    "autoprefixer": "^6.4.1",
    "browser-sync": "^2.16.0",
    "eslint": "^3.6.0",
    "eslint-config-mito": "^6.0.0",
    "gulp": "git+https://github.com/gulpjs/gulp.git#4.0",
    "gulp-clean-css": "^2.0.12",
    "gulp-htmlmin": "^3.0.0",
    "gulp-if": "^2.0.1",
    "gulp-imagemin": "^3.0.3",
    "gulp-load-plugins": "^1.3.0",
    "gulp-postcss": "^6.2.0",
    "gulp-sass": "^2.3.2",
    "gulp-sourcemaps": "^1.6.0",
    "panini": "^1.3.0",
    "rimraf": "^2.5.4",
    "yargs": "^5.0.0"
  },
  "engines": {
    "node": ">=4"
  },
  "pre-commit": [
    "pre-commit"
  ]
}
